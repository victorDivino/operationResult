# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

pool:
  vmImage: "windows-latest"

stages:
  - stage: Tool_Restore
    jobs:
      - job: Build
        steps:
          - script: dotnet tool restore

  - stage: Compile
    dependsOn: Tool_Restore
    jobs:
      - job: Build
        steps:
          - script: dotnet dotnet-cake --target=Build

  - stage: Test
    dependsOn: Compile
    jobs:
      - job: UnitTest
        steps:
          - script: dotnet dotnet-cake --target=UnitTest --exclusive

      - job: Coverage
        dependsOn: UnitTest
        steps:
          - script: dotnet dotnet-cake --target=Coverage --exclusive

  - stage: Deploy
    condition: eq(variables['Build.SourceBranchName'], 'master')
    jobs:
      - job: Publish
        steps:
          - script: dotnet dotnet-cake --target=Publish -configuration Release
